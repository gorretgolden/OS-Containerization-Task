version: "3.8"

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
    volumes:
      - .:/app
    # Uncomment below if you want to keep the container running even after you exit
    # stdin_open: true
    # tty: true
